import 'dart:ui';

import 'package:flutter/material.dart';

class Decor {
  static const blueshadow10 = BoxShadow(
      color: Color.fromRGBO(165, 200, 199, 0.4),
      blurRadius: 10,
      spreadRadius: 10);
  static const blueshadow25 = BoxShadow(
      color: Color.fromRGBO(165, 200, 199, 0.4),
      blurRadius: 10,
      spreadRadius: 25);
  static const blueshadow4 = BoxShadow(
      color: Color.fromRGBO(165, 200, 199, 0.4),
      blurRadius: 10,
      spreadRadius: 4);
  static const blueshadow20 = BoxShadow(
      color: Color.fromRGBO(165, 200, 199, 0.4),
      blurRadius: 10,
      spreadRadius: 20);
}
